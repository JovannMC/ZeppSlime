{
	"name": "zeppslime-monorepo",
	"private": true,
	"workspaces": [
		"./*"
	],
	"dependencies": {
		"@slimevr/firmware-protocol": "^0.6.6"
	},
    "scripts": {
        "dev:watch": "cd watch-app && zeus dev",
        "dev:server": "cd forward-server && bun run tauri dev"
    }
}
